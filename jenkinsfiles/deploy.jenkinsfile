pipeline {
    agent any

    tools {
        node 'Node14-17-0'
    }
    
    stages {
        stage('Deploy') {
            dir('./app') {
                steps {
                    sh 'npm install'
                    sh 'npm run build'
                }
            }
        }
    }
}
