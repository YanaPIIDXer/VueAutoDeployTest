pipeline {
    agent any

    tools {
        node 'Node14-17-0'
    }
    
    stages {
        stage('Deploy') {
            steps {
                dir('./app') {
                    sh 'npm install'
                    sh 'npm run build'
                }
            }
        }
    }
}
