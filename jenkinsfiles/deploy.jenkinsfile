pipeline {
    agent any

    tools {
        node 'Node14-17-0'
    }
    
    stages {
        dir('./app') {
            stage('Deploy') {
                steps {
                    sh 'npm install'
                    sh 'npm run build'
                }
            }
        }
    }
}
